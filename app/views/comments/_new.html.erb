<%= simple_form_for [@post, @comment] do |f| %>
  <%= f.input :content, as: :text, label: false, placeholer: "Write something kind" %>
  <%= f.submit 'Comment', class:'btn btn-rounded-blue' %>
<% end %>
